# miyako-invoice — AI開発アシスタント向けメモ

- ここは**父の本番業務システム**（main push＝即本番デプロイ）。CONTRIBUTING.md のルールに必ず従う
- main へ直接 push しない。PR経由・オーナー承認後のみ
- 消費税はテンプレート数式の8%（食品卸の軽減税率）が正。**変更禁止**
- 金額・税率・テンプレートのセル位置に触る変更は、実装前にオーナー確認が必須
- テスト：`python3 -m pytest test_app.py -q`（全グリーンでないとマージ不可）
- リモートの状態を語る前に必ず `git fetch`（stale情報での断定が過去に事故のもと）

# 開発ルール（miyako-invoice）

このリポジトリは **株式会社 新家（平戸家）の本番業務システム** です。
main ブランチ = 本番（push すると Streamlit Cloud に即デプロイされ、実際の請求書に影響します）。

## 絶対ルール

1. **main に直接 push しない。** 必ずブランチを切って Pull Request → オーナー（平戸凰雅）の承認後にマージ
2. **金額・税率・テンプレートの変更は、実装前にオーナーへ相談**（請求書の金額が変わる＝事故）
3. マージ前に **全テストがグリーン**であること：`python3 -m pytest test_app.py -q`（35件）
4. 仕様を変える場合は、父（利用者）の実際の使い方を確認してから。「良かれと思って」の再設計が2026-06-29に本番を止めた実例あり（d8ed22f → revert済み）

## 変更の流れ

```bash
git checkout -b feat/変更内容
# 実装 + テスト追加
python3 -m pytest test_app.py -q     # 35件以上・全グリーン
git push origin feat/変更内容
# GitHubでPRを作成 → オーナーのレビュー・承認 → マージ（＝自動デプロイ）
```

## システムの要点

- 入力：新家の納品書Excel（A3=店舗名 / N4=納品日 / V6:W11=価格マスタ / B18:B33×J列=明細）
- 出力：店舗別の請求書Excel（ZIP）。単価・金額は数値直書き（数式キャッシュに依存しない）
- 消費税は**テンプレート内の数式（8%・軽減税率）**で計算。食品卸なので8%が正しい（変更禁止）
- 価格マスタにない商品は、画面でその場入力して続行できる（¥0請求書は作れない設計）
- 品質ガード3重：単価不明チェック / 生成後の再読込検証 / テスト35件（本番と同一コードをテスト）

## 過去に踏んだバグ（同じ穴に落ちないために）

合計0円・日別明細崩れ・12行止まり・date型・テストと本番の実装乖離 の5件。
詳細はオーナーのメモに正本あり。テンプレートのセル位置を変えるときは必ず全件テスト＋実ファイルで確認。
